# Environment Variables Configuration
# Copy this file to .env and add your actual values

# ========================================
# Gemini API Configuration
# ========================================

# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Model selection: "gemini-2.5-flash" (fast, cheap) or "gemini-2.5-pro" (higher quality)
GEMINI_MODEL=gemini-2.5-flash

# ========================================
# Local Development Setup
# ========================================
# 1. Copy this file: cp .env.example .env
# 2. Add your real API key to .env
# 3. Never commit .env to git (it's in .gitignore)

# ========================================
# Streamlit Cloud Deployment Setup
# ========================================
# 1. Go to your app settings: https://share.streamlit.io
# 2. Navigate to: App Settings → Secrets
# 3. Add your secrets in TOML format:
#
#    GEMINI_API_KEY = "your_real_api_key_here"
#    GEMINI_MODEL = "gemini-1.5-flash"
#
# 4. Save and restart your app

# ========================================
# Security Notes
# ========================================
# - NEVER commit your real API key to git
# - Keep .env in .gitignore
# - Rotate keys if accidentally exposed
# - Use Streamlit secrets for cloud deployment
# - Monitor API usage and costs at: https://makersuite.google.com
